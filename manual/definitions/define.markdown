---
title: Value of Function Definition
layout: default
---

# Value of Function Definition

A definition introduces a new value or function.

A definition starts with keyword `define` followed by a name, the
equal sign, and an [expression](#expressions). A value is
defined like

    define name = expression;

and a function like

    define name(var,...,var) = expression;
